<nav class="branches">
  <ul class="branches__list">
    {{#each branches}}
      <li class="branches__item">
        <a
          {{#if class }}
            class="{{ class }}"
          {{else}}
            href="?branch={{ name }}&hash={{hash}}"
          {{/if}}
          >{{name}}</a>

        {{#if class }}
          {{> logs}}
        {{/if}}
      </li>
    {{/each}}
  </ul>
</nav>
